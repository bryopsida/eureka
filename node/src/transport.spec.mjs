import { ok } from 'node:assert'
import { describe, it } from 'node:test'

describe('EurekaServer', () => {
  it('should notify listeners when data from peer is received', () => {
    ok(false)
  })
  it('should send data to peer multicast group', () => {
    ok(false)
  })
})
